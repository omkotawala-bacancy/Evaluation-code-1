import React from 'react'

function Tag({tag}) {

    function handleClick(){
        
    }
  return (
    <div>
        <h1>tag.title</h1>
        <button onClick={handleClick}></button>
    </div>
  )
}

export default Tag